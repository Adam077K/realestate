'use client'

/**
 * HeroClouds — animated cloud/particle layer overlaying the Hero.
 * Currently a stub. Section worker: replace this component body
 * with Three.js / @react-three/fiber cloud implementation.
 */
export default function HeroClouds() {
  return (
    <section
      id="hero-clouds"
      aria-hidden="true"
      className="pointer-events-none absolute inset-0 z-5 min-h-[100px]"
    >
      {/* Hero clouds stub — Three.js implementation goes here */}
    </section>
  )
}
